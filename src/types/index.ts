export interface Position {
  x: number;
  y: number;
}

export interface BoardSize {
  rowCount: number;
  columnCount: number;
  aspectRatio: number;
}

export interface ScreenSize {
    width: number;
    height: number;
}
